# Event schema

All public events use `schemaVersion: "1.0"` and include event, trace, camera, session, timestamp, category, event code, phase, intent, confidence, duration, source, quality, and metadata fields. See `suha_core.domain.events.EventEnvelope` and the contract tests.

