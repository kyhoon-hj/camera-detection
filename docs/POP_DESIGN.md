# Stitch 참고 디자인 적용

2026-09-09, 사용자 제공 `stitch_wakeup_cam_video_alert.zip`의 `screen.png`, `code.html`, `DESIGN.md`를 참고했습니다. 첨부 HTML의 스크립트를 실행하거나 그대로 앱에 삽입하지 않았습니다.

## 범위

졸방 홈·영상 보관함에 우선 적용했습니다. 열공 빌드와 화면은 유지했습니다. 기존 통합 v6와 첨부 ZIP은 보존했습니다.

| 참고 디자인 | 앱 반영 |
|---|---|
| 보라색 바탕·라임·핑크 강조 | #14121d, #d2f000 및 핑크 보조색 |
| 큰 대표 영상 카드 | 현재 선택한 실제 경고 영상, 적용·보유 상태 표시 |
| 알약형 필터 | 실제 전체 11편 / 보유 영상 필터 |
| 세로 영상 목록 | 실제 기존 영상, 미리보기·선택 동작 |
| 하단 주요 버튼 | 기존 졸음 감지 시작 및 안전 안내로 연결 |
| 하드웨어 부스트 영역 | 기존 경보음·음성 안내 설정과 음성 테스트 |

참고 화면의 효과 수치, 소음 수치, 플래시, 웨어러블 연동, 파일 등록 기능은 실제 근거/구현이 없어 추가하지 않았습니다. 참고 화면의 원격 인물 이미지와 WakeDrive 로고 대신 기존 졸방 로고와 실제 앱 영상을 사용했습니다.

## 변경 파일

- src/DriverLibrary.tsx: 독립 홈 화면, 보유 필터, 선택·설정·미리보기 모달
- src/driver-library.css: 참고 팔레트와 영상 카드·하단 버튼 스타일
- src/main.tsx: 졸방 홈 연결, 기존 영상 적용/다운로드·음성 설정 연결, 네이티브 뒤로가기 시 미리보기 닫기

영상 선택만으로 현재 적용 영상을 바꾸지 않습니다. 대표 카드의 설정 버튼으로 적용합니다. 미리보기 닫기·ESC 및 네이티브 뒤로가기를 처리했습니다. 초기 디자인의 웹 즉시 받기 체험은 2026-09-10 잠금 요청으로 제거했습니다. 현재 잠금 정책과 검증은 [VIDEO_LOCK.md](VIDEO_LOCK.md)를 따릅니다.

## 검증

- 기존 감지 및 앱 분리 회귀: 13개 파일, 102개 테스트 통과
- TypeScript, 졸방 웹 빌드, Android assembleDebug 성공
- 보유 필터: 11편 → 보유 1편 표시 확인
- 로봇 확성기 선택: 대표 카드 변경, 미보유·받기 버튼 표시, 적용 영상은 기존 값 유지
- 실제 기본 영상 미리보기: 재생 완료 시간 약 10초, readyState 4, 미디어 오류 없음; 닫기 후 홈 복귀
- 브라우저 오류 로그 없음, 390px에서 콘텐츠 가로 넘침 없음
- 졸방 APK와 최종 웹 주요 자산 해시 일치; runtime-evidence.json 갱신
- 실제 기기의 감지 성능과 업데이트 APK 설치 후 동작은 이번 디자인 검증에서 수행하지 않음

설치 파일: releases/jolbang-debug.apk. 실행 주소: http://127.0.0.1:5210/.

## 2026-09-10 감지 내부 화면 추가 적용

홈만 바뀌고 실제 졸음 모드가 기존 디자인으로 남아 있던 누락을 수정했습니다.

- `src/driver-monitor.css`: 졸방의 카메라 준비 화면, 기준 측정 오버레이, 감지 상태 배지, 눈/고개 감지 카드, 상세 정보, 타임라인, 시작/정지 버튼, 설정, 안전 안내 및 구매 안내창까지 보라/라임/핑크 디자인 적용.
- `src/main.tsx`: 실제 실행 상태에 따른 상단 표시, 현재 적용된 경고 영상 표시. 기록이 없는 타임라인은 `기록 대기` 표시. 화면 전환 시 스크롤을 맨 위로 이동.
- 세로 화면의 시작/정지 버튼은 하단 고정, 가로 거치 화면에서는 오른쪽 패널 상단 고정. 위험 상태의 색상 구분과 경고 영상 재생 처리는 유지.
- 적용 범위는 졸방입니다. 열공 화면, 감지 알고리즘과 임계값은 변경하지 않았습니다.

검증: 기존 102개 테스트 통과. 390×844 세로 및 844×390 가로 브라우저에서 가로 넘침 없음, 내부 준비 화면·안전 안내·상세 정보 펼침·설정 이동 및 복귀 확인. TypeScript/웹 빌드 및 Android APK 빌드 검증. 최종 APK 내 웹 자산 일치는 `runtime-evidence.json`에 기록.

실제 카메라를 켠 상태의 기준 측정·위험 알림 화면과 실기기 설치는 이번에 검증하지 않았습니다. 해당 상태의 UI 스타일은 적용했으며, 감지 기능 테스트 통과를 실기기 검증으로 간주하지 않습니다.
